Configuration:
  status: info

  appenders:
    Console:
      name: LogToConsole

Loggers:
  logger:
    - name: lesson32
      level: info
      AppenderRef:
        - ref: LogToConsole
  Root:
    level: info
    AppenderRef:
      ref: LogToConsole
